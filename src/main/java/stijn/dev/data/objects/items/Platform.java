package stijn.dev.data.objects.items;

public class Platform {
    String defaultFolder;

    private String name;
    Emulator defaultEmulator;

    public Platform(String name, String defaultFolder, Emulator defaultEmulator){
        this.defaultEmulator = defaultEmulator;
        this.defaultEmulator = defaultEmulator;
        this.name = name;
    }
}
